# graduation_project_admin_dashboard

A new Flutter project.

## Deployment

## steps:

`flutter clean`
`flutter pub get`
`flutter build web --release`
then add it to another repo within the build web
`cd build\web`
`git init`
`git add .`
`git commit -m "Deploy 1 "`
`git remote <your project url>`
create another
